# Child flake lockfiles -- deps follow from tests/dependencies/flake.lock
tests/unit/flake.lock
tests/integration/*/flake.lock
tests/integration/*/*/flake.lock
examples/*/flake.lock

# Claude Code
/.claude/

# Built by CI (mdBook build-dir); never committed
/site/docs/
