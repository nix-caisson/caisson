# SPDX-License-Identifier: MIT
#
# The `default` flake module caisson registers. The configuration
# name and the lib export it used to declare are part of caisson's
# core module now, applied to every evaluation; this module stays
# registered, empty, for the consumers that select it by name.
{ ... }:
{ ... }:
{
}
